

# The following are the packages used in the project.
# You can install them using pip by running the following command in your terminal:
#         pip install -r requirements.txt

flask >= 2.0.0  # incase you want to specify a specific version of flask, you can do so by changing the version number and use the 
# double equal sign (==) instead of the greater than or equal to sign (>=). For example, if you want to use flask version 2.0.1
# This can also apply to the other packages below:
pandas >= 1.3.0
numpy >= 1.21.0
scikit-learn >= 0.24.0
matplotlib >= 3.4.0
seaborn >= 0.11.0
joblib >= 1.0.0
gunicorn >= 20.1.0